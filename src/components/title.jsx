import { danfo } from "@/app/layout";

export default function Title() {
  return (
    <h1 className={`${danfo} text-6xl`}>
      <span className="text-blue-500 text-7xl">s</span>udok
      <span className="text-blue-500 text-7xl">u</span>
    </h1>
  );
}
